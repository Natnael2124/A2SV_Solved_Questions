n, s = map(int, input().split())
a = list(map(int, input().split()))

ans = 0
Window = 0 
left = 0 

for right in range(n):
    Window += a[right]
    
    while Window > s:
        Window -= a[left]
        left += 1
          
    ans += (right - left + 1)

print(ans)
